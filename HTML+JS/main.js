 // Chuỗi 
// var myString = 'Tự học JS và tra thêm về JS'

// console.log(myString.lastIndexOf('JS'))
// console.log(myString.slice(7,9));
// console.log(myString.toUpperCase());
// console.log(myString.replace(/JS/g, 'JavaScrip'));


// Mảng
    var language = [
        'JavaScrip',
        'PHP',
        'Ruby',
    ];
    var language2 = [
        'HTML',
        'CSS'
    ]
    
    // console.log(language.concat(language2)); // concat: nối  mảng lại vs nhau
    // console.log(language.slice(1, 3));
    // console.log(language.splice(1, 1, 'ReactJS', 'Java'));
    // console.log(language)
//

// Hàm
    /*function showDialog() {
        var myString = '';
        for(var param of arguments) {
            myString += `${param} -`;
        }
        console.log(myString);  
    }
    showDialog('log 1', 'log 2', 'log3');
    
            RETURN
    function cong(a, b) {
        return a * b ;
    }

    var result = cong(2, 8);
    console.log(result);*/

// End Hàm

// var number = 5;
// output = ++number + number-- ;
// console.log(output);